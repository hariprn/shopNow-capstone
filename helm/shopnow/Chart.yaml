apiVersion: v2
name: shopnow
description: ShopNow e-commerce application Helm chart
type: application
version: 0.2.0
appVersion: "1.0.0"

dependencies:
  - name: kube-prometheus-stack
    version: "87.18.1"
    repository: "https://prometheus-community.github.io/helm-charts"
    condition: monitoring.enabled
