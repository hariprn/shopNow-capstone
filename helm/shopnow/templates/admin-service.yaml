apiVersion: v1
kind: Service
metadata:
  name: admin
spec:
  type: {{ .Values.admin.service.type }}

  selector:
    app: {{ .Release.Name }}-admin

  ports:
    - port: {{ .Values.admin.service.port }}
      targetPort: 80
      protocol: TCP
