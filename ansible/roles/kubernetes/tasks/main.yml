---
- name: Verify AWS CLI installation
  command: aws --version
  register: aws_version
  changed_when: false

- name: Verify kubectl installation
  command: kubectl version --client
  register: kubectl_version
  changed_when: false

- name: Configure kubectl for ShopNow EKS cluster
  become: false
  command: >
    aws eks update-kubeconfig
    --region {{ aws_region }}
    --name {{ eks_cluster_name }}
  environment:
    HOME: /home/{{ ansible_user }}
  register: kubeconfig_result
  changed_when: "'Updated context' in kubeconfig_result.stdout"

- name: Check ShopNow namespace connectivity
  become: false
  command: kubectl get pods -n shopnow
  environment:
    HOME: /home/{{ ansible_user }}
  register: kubectl_namespace
  changed_when: false

- name: Display ShopNow namespace resources
  debug:
    var: kubectl_namespace.stdout_lines
